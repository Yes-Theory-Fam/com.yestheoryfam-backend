// Not ideal again; need to look into what's wrong with this but I wanted to get initial commit out as early as possible.
/* eslint-disable no-unused-vars */
enum Error {
  UNAUTHORIZED,
  UNEXPECTED_RESPONSE_CODE,
}

export default Error;
